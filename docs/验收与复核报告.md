# TG115 一键部署器：v1.5.0 全面审核与验收报告

> 本文为历史版本记录；当前 v1.6.1 改动与验证见
> [v1.6.1 更新与验收记录](v1.6.1-更新与验收.md)，不要沿用下文作为新版验收结论。

> 版本：1.5.0
> 复核日期：2026-07-29

## 一、结论

v1.5.0 已修复本轮三次审核中确认的代码问题：超过本地预算的单文件永久排队、上传最后阶段
取消产生远端孤儿文件、下载改名时崩溃产生本地孤儿文件、缺失文件继续占用额度，以及
CloudDrive2 容器误识别。

当前源码已通过 75 项自动化测试、Python 编译、Ruff、Bandit、pip-audit、Windows EXE
打包自检，以及 GitHub Actions 中的 Linux ShellCheck、Compose 校验和 Bot 镜像构建。

这些检查证明程序逻辑和交付包在可自动验证范围内成立，但不能替代以下真实验收：

1. CloudDrive2 是闭源特权容器，无法进行源代码级安全证明；
2. CloudDrive2 WebDAV 和 115 最终入库行为必须在真实 VPS 上确认；
3. 新增流式模式必须用真实的大文件完成一次 Telegram → VPS → CloudDrive2 → 115 验收。

## 二、最终处理规则

默认环境：

- VPS：2 核 CPU、4GB 内存、50GB 硬盘；
- 普通落盘任务预算：20GB；
- 磁盘最少保留：20GB；
- 不设置人工固定的下载或上传任务数；
- 并发由 CPU、内存、磁盘、网络、错误率和目的端健康状态动态调节。

文件按以下规则自动分流：

| 文件情况 | 处理方式 | 是否占用完整本地额度 | 失败后的恢复方式 |
|---|---|---:|---|
| 单文件不超过本地预算 | 完整下载到 VPS，再上传 | 是 | 保留完整本地文件并重试上传 |
| 单文件超过本地预算 | Telegram → rclone → CloudDrive2 流式传输 | 否 | 清理临时远端文件，从头重试 |

流式模式通过 `rclone rcat --size` 提供准确文件大小，并使用异步标准输入背压，避免把整个
文件放入内存。远端仍使用临时名称，传输完成后检查大小，再改成正式文件名。

## 三、必须保持的不变量

1. 普通落盘任务按完整文件大小原子预留，合计不超过本地任务预算；
2. 单个文件超过预算时自动流式处理，不要求用户重新部署或调大预算；
3. 磁盘到达安全线时不继续开启传输，CloudDrive2 缓存也受真实剩余空间保护；
4. CPU 或内存持续高压时收缩动态窗口，不继续无上限放行；
5. 用户一次转发即可，未开始任务自动留在队列，不需要重新转发；
6. 远端文件通过大小校验并安全改名后才标记 Bot 传输完成；
7. 普通模式在远端确认前不删除 VPS 完整副本；
8. `/cancel` 只有在本任务远端文件清理成功后才删除本地副本并标记取消；
9. 重启发生在下载、上传、流式传输、远端改名或本地清理阶段时，任务可恢复或重新排队；
10. 文件进入 WebDAV 根目录或所填相对子目录，不建立年份和月份文件夹；
11. 同名文件不覆盖，自动追加任务编号；
12. Bot 只允许配置的 Telegram 数字 ID 使用；
13. “Bot 传输完成”和“115 官方客户端已确认”始终分开表达。

## 四、本轮修复和对应测试

| 修复项目 | 根因 | 修复方式 | 回归结果 |
|---|---|---|---|
| 大于 20GB 的单文件永久排队 | 数据库直接拒绝大于预算的任务 | 新增 `transfer_mode=stream` 和 rclone 流式写入 | 通过 |
| 流式传输占用本地额度 | 所有活跃状态都按完整文件计费 | 流式任务从本地任务额度查询中排除 | 通过 |
| 流式中断留下临时远端文件 | 没有流式清理与重试协议 | 终止子进程、清理临时文件、重新排队或失败保留记录 | 通过 |
| 远端改名后执行 `/cancel` 留下孤儿文件 | 取消逻辑使用旧数据库快照，只删除临时名 | 取消后重新读取状态，验证并删除记录的真实远端路径 | 通过 |
| 远端删除失败仍删除本地文件 | 取消流程没有失败关闭 | 远端无法确认清理时中止取消并保留本地副本 | 通过 |
| 下载改名后崩溃留下本地孤儿 | 文件改名早于最终路径持久化 | 改名前先登记确定性最终路径，恢复时识别旧孤儿文件 | 通过 |
| 缺失的保留文件继续占用额度 | `verification_failed_retained` 始终按完整大小计费 | 自动重新排队并清除失效本地路径 | 通过 |
| 流式任务重启后重复写入 | 恢复时不检查已存在远端正式文件 | 先验证数据库记录的远端文件，再决定续接或重传 | 通过 |
| 19798 端口容器被误认成 CloudDrive2 | 旧检测把端口当作身份 | 只按容器名和镜像识别；端口身份不明时拒绝修改 | 通过 |
| 旧数据库不认识流式模式 | 缺少模式字段 | 启动时自动迁移并默认旧任务为 `local` | 通过 |

## 五、用户场景模拟

| 场景 | 结果 |
|---|---|
| 1 个普通视频完成下载、上传、校验、改名和清理 | 通过 |
| 单文件超过 20GB 自动切换流式模式 | 通过 |
| 流式文件不创建 VPS 完整副本、不占用完整本地额度 | 通过 |
| 流式重启时发现远端正式文件，跳过 Telegram 重下 | 通过 |
| 15 个 4GB 文件按 20GB 预算分批 | 通过 |
| 7 个 3GB 文件首批放行 6 个 | 通过 |
| 40 个并发预留不突破 20GB | 通过 |
| 100 个混合任务自动多轮处理 | 通过 |
| 前 200 个大任务阻塞时后面小任务不饿死 | 通过 |
| CPU、内存或磁盘压力触发窗口收缩 | 通过 |
| CloudDrive2 不健康时不开始新传输 | 通过 |
| 上传失败保留普通模式本地完整文件 | 通过 |
| 远端改名瞬间取消，不留下远端孤儿 | 通过 |
| 远端文件无法删除时，取消失败关闭并保留本地文件 | 通过 |
| 下载改名和数据库提交之间崩溃 | 通过 |
| 缺失保留文件自动释放额度 | 通过 |
| 同名文件不覆盖，不建立年份或月份目录 | 通过 |
| 非授权 Telegram 用户发送文件 | 拒绝 |
| 公网明文 HTTP WebDAV | 部署前拒绝 |
| 旧版数据库升级 | 通过 |
| EXE 首次打开时凭据输入框为空 | 通过 |

## 六、工具检查

| 检查 | 结果 |
|---|---|
| Python 自动化测试 | 75/75 通过 |
| Python `compileall` | 通过 |
| Ruff 0.16.0 | 通过 |
| Bandit 1.9.4 | 0 个阻断项 |
| pip-audit 运行依赖 | 0 个已知漏洞 |
| pip-audit 构建依赖 | 0 个已知漏洞 |
| GitHub Actions Windows 测试和 EXE 自检 | 通过 |
| GitHub Actions Linux ShellCheck | 通过 |
| GitHub Actions Compose 校验 | 通过 |
| GitHub Actions Bot 镜像构建 | 通过 |
| 发布文件 SHA-256 | 0 个不一致 |
| 源码和发布包秘密扫描 | 0 个真实凭据 |

## 七、仍然存在的边界

### 1. CloudDrive2 特权容器

CloudDrive2 为 FUSE 挂载使用 `privileged: true` 和宿主机 PID 命名空间。这是高权限边界，
建议 VPS 只运行本项目，不与钱包、数据库或其他重要服务共用。

### 2. CloudDrive2 缓存不受 20GB 数据库额度直接控制

20GB 约束的是 Bot 普通落盘任务。流式模式不创建完整 VPS 文件，但 CloudDrive2 仍可能为
115 后台上传使用自己的缓存。系统根据整个分区的真实剩余空间停止新传输，但无法证明闭源
缓存永远保持在固定大小。

### 3. 流式模式不能断点续传

流式数据没有完整保存在 VPS。单次 rclone 流式上传不能从任意中点恢复；网络中断后程序会
清理临时远端文件并从 Telegram 开头重试。这是节省磁盘与失败恢复成本之间的明确取舍。

### 4. 远端仅校验大小

普通 WebDAV 对该目的端没有稳定内容哈希。程序校验本地大小、临时远端大小和正式远端大小，
能发现截断和常见写入失败，但不能数学证明同大小内容绝对没有位翻转。

### 5. 115 最终状态需要人工确认

Bot 能证明 CloudDrive2 WebDAV 已接收并通过大小校验，不能调用可信的 115 官方接口判断
最终入库。用户在 115 官方客户端看到文件完整且可以播放后，使用 `/confirm <编号>` 记录。

### 6. Windows EXE 未进行商业代码签名

发布包提供 SHA-256，但没有商业 Authenticode 证书，Windows SmartScreen 或安全软件可能
提示未知发布者。用户应从本仓库 Release 下载并核对哈希。

## 八、真实 VPS 最终验收

1. 使用 v1.5.0 部署器重新部署，确认 `TG115_RESULT=SUCCESS`；
2. 确认 CloudDrive2 登录、115 挂载和原任务数据库仍在；
3. 执行 WebDAV 验收，确认 `TG115_DESTINATION=OK`；
4. 转发一个普通小文件，确认普通落盘流程完成；
5. 转发一个超过所设本地预算的测试文件，确认状态显示流式模式；
6. 传输期间检查 VPS 下载目录，确认没有该大文件的完整副本；
7. 确认远端临时名称最终变成正常文件名；
8. 在 115 官方客户端确认大小和播放；
9. 测试一个传输中取消任务，确认本地和本任务远端临时文件都不存在；
10. 重启 VPS，确认队列和已完成任务状态恢复。

只有以上真实链路全部通过，才能称为当前 VPS 上的端到端生产验收完成。
